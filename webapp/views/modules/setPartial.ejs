
<%# partial to store some summary data about expansions and sets in each expansion %>
<div class="container position-relative px-0 border-1 rounded-2 border-dark-subtle border px-3 py-2 bg-light-subtle ">
    

    <% let collectedPercentage = Math.floor(set.userCollectedCount * 100 /  set.total_cards) || 0  %>

    <div class="row mb-3">
        <span class="col-auto ">
            <%= set.name %> |
            <small><%= set.code %></small>
        </span>
        <div class="col d-flex justify-content-sm-end">
            <small class="mb-0"><%= dateFunctions.formatDate(set.release_date) %></small>
        </div>
    </div>

    <div class="row align-items-center mb-2">
        <img class="setLogo col-lg-6 col-md-5 col-4 img-fluid" src=<%= set.icon_url %>>
    </div>

    <div class="row mb-2 d-flex align-items-center ">


        <div class="col-12 col-sm-7">
            <div class="d-flex  justify-content-between align-items-center">            
                <div>
                    <small > <%= set.userCollectedCount ||0 %> / <%= set.total_cards %></small> <%# need to update this for users, when built that functionality in  %>
                </div>

                <div class="">
                    <small ><%= collectedPercentage %> %</small> <%# need to update this for users, when built that functionality in  %>
                </div>
            </div>

            <div class="progress " style="height: 10px;">
                <div class="progress-bar bg-primary " role="progressbar" style="width: <%= collectedPercentage %>%; height: 10px;" aria-valuenow=<%= 25 %> aria-valuemin="0" aria-valuemax="100"></div>
            </div>
        </div>

        <div class="col-12 col-sm-5 d-flex justify-content-center   justify-content-sm-end my-2 my-sm-0   ">
            <a href="/cards/sets/<%= set.id %>" class="btn actionBtn btn-sm btn-light ">View Set</a> <%# need to update to bring to seperate page %>
        </div>
        
          
          

    </div>
    

</div>

